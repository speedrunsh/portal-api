name: Portal API
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  make-all:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: go
        run: make go
      - name: Validate clean git state
        run: git diff --exit-code